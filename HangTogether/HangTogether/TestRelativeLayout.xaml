<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pancakeView="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             xmlns:hangTogether="clr-namespace:HangTogether;assembly=HangTogether"
             x:Class="HangTogether.TestRelativeLayout">
    
    <RelativeLayout>
<!-- Pour YConstraint % a partir du haut de l'ecran et r XConstraint % a partir du coin
gauche de l'ecran-->
        <!-- XConstraint : ou ca commence par rapport a la LARGEUR de l'ecran
Si xConstraint Factor = 0.1 et WidthFactor = 1; ca commence a 0.1% de l'ecran et termine
a la valeur de WidthFactor(1) == toute l'ecran -->

<!-- YConstraint et HeightConstraint pareil sauf que ca  se ft en fonction de hauteur de l'ecran -->

        <!-- Centered and 1/3 width and height of parent -->
        <BoxView x:Name="oneThird"
                 Color="Silver"

                 RelativeLayout.YConstraint="{ConstraintExpression Type=RelativeToParent, Property=Height, Factor=0.98}"
                 RelativeLayout.WidthConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width, Factor=1}" />


    </RelativeLayout>
</ContentPage>